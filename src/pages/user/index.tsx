import {Button} from "@chakra-ui/react";

export default function Index() {
    return (
        <div style={{padding: 50}}>
            <h1>Profiles</h1>
            <h2>ex name</h2>

            <Button h={180} w={180} borderRadius={100}>NK</Button>
        </div>
    )
}